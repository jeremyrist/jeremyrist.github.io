##How to run `node-sass` in the most efficient way

Do something like this:

`node-sass input output`

or

`node-sass current-file location-file`

or 

`node-sass src/scss/main.scss src/css/main.css`
